[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "supertask"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "colorama",
    "dateparser",
]

[project.scripts]
st = "todo.main:main"
